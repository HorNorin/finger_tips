<div class="row form-container">
  <div class="col-xs-12 col-md-8 col-md-offset-2">
    <h2><%= title %></h2>
    
    <%= form_for [:admin, @lesson], html: {class: "form"} do |f| %>
      <%= render "shared/error_messages", object: f.object %>
    
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: "form-control" %>
      </div>
      
      <!-- <div class="form-group">
        <div>
          <%#= f.fields_for :episodes do |builder| %>
            <%#= render "episode_fields", f: builder %>
          <%# end %>
        </div>
        <%#= link_to_add_fields "Add Episode", f, :episodes %>
      </div> -->
      
      <div class="form-group">
        <%= link_to "Back", admin_lessons_path, class: "btn btn-default pull-left" %>
        <%= f.submit @lesson.new_record? ? "Save" : "Update", class: "btn btn-danger pull-right" %>
        <div class="clearfix"></div>
      </div>
    <% end %>
  </div>
</div>
